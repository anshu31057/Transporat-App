import PageCard from '../components/PageCard';

const EditEntryPage = () => {
  return (
    <PageCard title="Edit Entry">
      <p className="text-base text-slate-600">Select a record from the entries list to edit.</p>
    </PageCard>
  );
};

export default EditEntryPage;
